# Pipecat with the plugins this agent uses:
#   silero    -> Silero VAD (endpointing + barge-in)
#   whisper   -> local faster-whisper STT
#   anthropic -> Claude LLM
#   openai    -> OpenAI-compatible LLM client; used to talk to a self-hosted
#                Ollama server (e.g. Mistral) via its /v1 endpoint
#   websocket -> FastAPI WebSocket transport + Protobuf serializer
#   runner    -> dev/prod FastAPI server providing /start and /ws-client
#   piper     -> required for the module that holds PiperHttpTTSService (the HTTP
#                client; the actual synthesis runs in the separate tts container)
pipecat-ai[silero,whisper,anthropic,openai,websocket,runner,piper]>=1.3.0
uvicorn>=0.30
python-dotenv>=1.0
redis>=5.0
asyncpg>=0.29
aiohttp>=3.9
loguru>=0.7
